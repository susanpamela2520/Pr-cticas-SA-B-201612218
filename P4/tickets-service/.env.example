PORT=8001
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/tickets_db

# Debe ser EXACTAMENTE el mismo valor que JWT_SECRET en auth-service:
# ambos servicios firman/verifican con el mismo secreto compartido.
JWT_SECRET=cambia-esta-clave-por-una-propia-y-larga

NOTIFICACIONES_URL=http://localhost:8002
